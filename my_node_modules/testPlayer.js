var c = require("./cards.js");
var cards = c.cards;
var Card = c.Card;

/******* Affordability checks ******/
exports.testCostLower = function (test) {
    var card = cards.market;
    test.strictEqual(card.getCost(Card.locations.LOWER_BOARD), card.cost - 1, "reduced cost from lower board");
    test.done();
};
